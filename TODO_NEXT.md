YOLOInventory Next TODOs

- Implement Dungeon Siege-style hierarchical loot:
  - nested/sub loot tables,
  - category buckets,
  - level-band weighting,
  - context-based filters (enemy/archetype/region).
- Add generator support for direct row/code entries in loot tables (without requiring pre-generated assets).
- Add dashboard tools to build and validate DS-style drop graphs.
- Add generator simulation analytics panel (distribution charts over many runs).
